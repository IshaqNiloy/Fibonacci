def fibo(n):
    if n == 0:
        return 0
    elif n == 1:
        return 1
    else:
        return fibo(n - 1) + fibo(n - 2)

txt = "The {}th Fibonacci number is {}"

num = int(input("Which Fibonacci number you want to get?"))
print(txt.format(num, fibo(num)))
